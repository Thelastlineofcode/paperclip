# Lois — Heartbeat

1. Pull support tasks: `GET /api/v1/agents/lois/tasks`
2. Check for open guest-facing issues: `gh issue list --repo Thelastlineofcode/jouvae --label guest --state open`
3. Escalate product/technical gaps to dorothy
4. Append to `/job/SUMMARY.md`: `[lois] <timestamp> — <summary>`
