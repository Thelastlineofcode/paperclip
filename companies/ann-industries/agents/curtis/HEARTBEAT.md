# Curtis — Heartbeat

1. Pull partner tasks: `GET /api/v1/agents/curtis/tasks`
2. Check partner onboarding queue — track open requests to completion
3. Surface blockers early to dorothy
4. Append to `/job/SUMMARY.md`: `[curtis] <timestamp> — <summary>`
