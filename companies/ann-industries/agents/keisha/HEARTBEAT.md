# Keisha — Heartbeat

1. Pull implementation tasks: `GET /api/v1/agents/keisha/tasks`
2. For each task: write RED test first, implement until GREEN, open PR to `main`
3. No implementation without Marcus-approved contract — block if spec is missing
4. Branch: `keisha/job-<slug>-<date>`, PR title: `[keisha] <task>`
5. Append to `/job/SUMMARY.md`: `[keisha] <timestamp> — <summary>`
